%Ejemplo 2.3.5

    function [numero, determinante] = ejemplo 1 (x)
[n,m] = size (x);
numero= n*m;
determinante = det(x); fprintf ('El numero de elementos de la matriz es: ')
numero
fprintf ('El determinante de la matriz es: ')
determinante
    end
